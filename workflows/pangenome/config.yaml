# Input paths (relative to workflow directory or absolute)
reference: "../../data/test/reference.fa"
# New: Path to the directory containing haplotype FASTA files generated by vcf_generation
haplotype_data_dir: "../../results/variant_data"

# Output directory
output_dir: "../../data/test/pangenome"

# VG parameters
min_node_length: 32
vg_construct_opts: ""  # Additional options for vg construct

# Resources
threads: 4

# Optional features
create_indexes: true
create_visualization: true  # Only for small graphs
